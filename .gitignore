# ── Resultados e dados ────────────────────────────────────────────────
results/
results-teste/
fastqs/
*.fastq.gz
*.fq.gz
*.bam
*.bai
*.sam

# ── Snakemake runtime ─────────────────────────────────────────────────
.snakemake/
*.log
logs/

# ── Python ────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/

# ── Documentos internos (não para publicação) ─────────────────────────
README-old.md
README-new.md
ROADMAP.md
datasets-validacao.md
docker-tools.md
tools-parameters.md
tutorial_docker_singularity_debian.md
results-reference/
helps/
skills-lock.json

# ── Diretório separado (build artifacts) ─────────────────────────────
vapor/

# ── Ambientes de desenvolvimento (conda -p local) ─────────────────────
.envs/

# ── Containers ────────────────────────────────────────────────────────
# containers.lock.yaml SHOULD be committed (pins exact image tags)
# Apptainer image cache — large SIF files, do not commit
.apptainer/
singularity/
*.sif

# ── Misc ──────────────────────────────────────────────────────────────
*.svg
*.pdf
*.html
!scripts/report/components/shell.html
!scripts/report/components/shell_v2.html
*.zip
*.tar.gz
.DS_Store
check_containers.sh

# Baseline de regressão do catálogo de vOTU (regenerável — ver README no diretório)
tests/data/votu_regression/*.fasta
tests/data/votu_regression/*.tsv

# Workspace do subagent-driven-development (scratch, por plano)
.superpowers/

# ── Pipelines de referência clonados para comparação ──────────────────
# Repositórios git embutidos. Sem isto, um `git add -A` distraído varre
# milhares de arquivos de terceiros para dentro de um commit nosso.
metaFun/
phanta/
vOMIX-MEGA/
viroprofiler/
.agents/
.cache/
.claude/
